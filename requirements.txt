# MOSEv2 作业依赖
# 安装: uv pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 注意: PyTorch 需要单独安装（指定 CUDA 版本）:
#   uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# SAM2 核心依赖
numpy>=1.24.4
tqdm>=4.66.1
hydra-core>=1.3.2
iopath>=0.1.10
pillow>=9.4.0

# 可视化 & 调试（可选）
matplotlib
opencv-python
